import React from 'react'
import PropTypes from 'prop-types'

const ProfilesFeedback = ({ isLoading, error, profiles, onDelete, onLike }) => {
    // if (isLoading) return <Spinner />
    // if (error) return <Error errorMessage={error} />

    return (
        <div>ProfilesFeedback</div>
    )
}

ProfilesFeedback.propTypes = {}

export default ProfilesFeedback